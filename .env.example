# Signal Bot Configuration
# Copy this file to .env and update with your phone number
# Run: ./setup.sh for automatic configuration

# Your Signal phone number with country code
# Format: +[country_code][number]
# Example: +64274757293
PHONE_NUMBER=+64XXXXXXXXXX

# Signal username (same as phone number)
SIGNAL_USERNAME=+64XXXXXXXXXX

# Seller Signal ID (same as phone number for self-service bot)
SELLER_SIGNAL_ID=+64XXXXXXXXXX

# Database path
DATABASE_PATH=data/shop.db

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=DEBUG

# Server configuration
HOST=0.0.0.0
PORT=5000

# signal-cli daemon settings (JSON-RPC over TCP)
# The bot starts signal-cli in daemon mode to avoid subprocess hanging issues.
# SIGNAL_DAEMON_PORT: TCP port for the daemon (default: 7583)
# SIGNAL_DAEMON_STARTUP_TIMEOUT: seconds to wait for daemon to be ready (default: 30)
SIGNAL_DAEMON_PORT=7583
SIGNAL_DAEMON_STARTUP_TIMEOUT=30
